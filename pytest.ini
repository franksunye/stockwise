[pytest]
testpaths = backend/tests
markers =
    unit: fast isolated tests
    integration: tests that exercise filesystem, sqlite, subprocesses, or multi-module flows
    network: tests that require external services or live network access
