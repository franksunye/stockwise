# 线上 DeepSeek 与本地 B2_PROD_SAFE DeepSeek 对照（2026-03-12）

## 1. 对照对象

- 同一标的：`300502`
- 同一日期：`2026-03-12`
- 对照一：
  - 线上真实生产结果
  - 来源：[`Raw_Production_Response.json`](/Users/yesun/Code/stockwise/docs/7_Debug_Traces/results/Raw_Production_Response.json)
- 对照二：
  - 本地主链路重跑结果
  - 模型：`deepseek-v3`
  - 模板：`B2_PROD_SAFE` (`prompt_version = b2.v1`)

## 2. 核心指标对照

| 维度 | 线上真实 DeepSeek | 本地 `DeepSeek + B2_PROD_SAFE` | 观察 |
|---|---|---|---|
| `signal` | `Side` | `Side` | 主信号一致 |
| `confidence` | `0.45` | `0.4` | 置信度非常接近，本地更保守 |
| `prompt_version` | `v3.6` | `b2.v1` | 模板不同 |
| `layer1_status` | `RiskOff` | `RiskOff` | 风险语义一致 |
| `summary` 主旨 | 观望，缺乏高赔率入场点 | 防守，等待企稳信号 | 本地版更明确写出 `RiskOff` 约束 |

## 3. 摘要与结论对照

| 维度 | 线上真实 DeepSeek | 本地 `DeepSeek + B2_PROD_SAFE` |
|---|---|---|
| `summary` | 股价放量跌破MA20，主力大幅流出，短期趋势转弱，缺乏高赔率入场点，建议观望。 | 股价放量跌破关键均线，主力大幅流出，短期趋势转弱。量化状态为RiskOff，应优先防守，等待企稳信号。 |
| `trend` 结论 | 短期趋势由多转空。 | 短期上升趋势破坏，进入调整。 |
| `momentum` 结论 | 动能指标全面转空。 | 动量指标与资金流向均偏空。 |
| `levels` 结论 | 处于支撑与压力之间的真空地带。 | 价格处于关键支撑位边缘，方向待定。 |
| `context` 结论 | 外部环境对科技股不利。 | 市场环境与系统约束均不支持进攻。 |
| `decision` 结论 | 无高期望值机会，维持观望。 | 选择观望，等待更明确的企稳或突破信号。 |

## 4. 反方观点与明日关注点

| 维度 | 线上真实 DeepSeek | 本地 `DeepSeek + B2_PROD_SAFE` | 观察 |
|---|---|---|---|
| `counter_argument` | 若能在 `366.11` 附近企稳，可能形成中期底部，存在左侧博弈机会。 | 中期机会位较低，短期均线仍有多头排列，存在超跌反弹可能。 | 两边都承认“有反弹可能，但不是当前主结论” |
| `tomorrow_focus` | 关注 `366.11` 支撑及能否收复 `MA20(381.14)`。 | 关注 `375.67` 支撑有效性及能否收复 `381.14(MA20)`。 | 本地版把近端支撑看得更近、更谨慎 |

## 5. 战术动作对照

| 维度 | 线上真实 DeepSeek | 本地 `DeepSeek + B2_PROD_SAFE` | 观察 |
|---|---|---|---|
| `holding_profit[0]` | `减仓或离场` | `持仓观察` | 本地版对盈利持仓更温和 |
| `holding_loss[0]` | `严格止损` | `严格止损` | 核心防守动作一致 |
| `empty[0]` | `保持观望` | `等待观察` | 语义一致 |
| 空仓触发 | 无明确信号，等待企稳 | 站回 `MA20` 且量温和放大 | 本地版更偏右侧确认 |

## 6. 当前判断

| 结论项 | 判断 |
|---|---|
| 主方向是否漂移 | 没有，仍是 `Side` |
| 风险约束是否保留 | 保留，而且本地版写得更显式 |
| 保守程度 | 本地 `B2_PROD_SAFE` 版更保守 |
| 与线上既有风格的一致性 | 整体一致，但战术口吻更“等待确认” |
| 是否出现明显行为回归 | 没有看到明显回归 |

## 7. 更严谨的结论

- `B2_PROD_SAFE` 放到 `deepseek-v3` 上，没有改变本案例的主方向判断。
- 本地重跑结果与线上真实 DeepSeek 在 `signal`、`confidence`、防守倾向上高度一致。
- 相比线上 `v3.6`，本地 `b2.v1` 更显式地表达了 `RiskOff` 约束，且对动作触发更偏右侧确认。
- 当前更像是“保守风格强化”，而不是“策略立场改变”。
