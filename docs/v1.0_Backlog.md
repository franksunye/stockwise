# 🛰️ StockWise v1.0 “逻辑先行”研发积压工作清单 (Backlog)

> **目标**：交付首个具备“逻辑研判”闭环的正式版本 (v1.0 ZH)，建立用户对 AI 推演的底层信任。

---

## 🟥 P0: 核心发行阻断 (Core Rollout - v1.0 ZH)

### 1. 前端：研判中心化 (UI Consolidation)
- [x] **Tab 逻辑合并**：在 `TacticalBriefDrawer.tsx` 中移除“择时/决议”Tab，合并入 `brief` Tab。
- [x] **管理页静默**：基于 `tier !== 'pro' && tier !== 'alpha'` 隐藏 Header 中的 `management` 标签（v1.0 全系列静默）。
- [x] **文案与视觉统一（研判域）**：
  - 中文“逻辑研判”统一为“研判报告”（英文保持不变）；
  - “查看策略推演过程”简化为“策略推演过程”；
  - 单 Tab 状态下“研判报告”样式与多 Tab 的选中态保持一致。
- [x] **投研报告分层收敛（v1）**：
  - `free/go/plus` 不展示“投研决议”区块；
  - 对应数据请求同步抑制，避免无效拉取；
  - 海报“当前价”字号收敛至 `1.0rem`。

### 2. 后端：多级智力分发 (Multi-tier Reasoning Support)
- **Story: 激活 Go (DeepSeek) 研判通路**
    - [ ] **Work Item（Deferred）**: `briefs` 功能整体后置，Go 不新增独立提示词模板（不引入 `system_go.j2`）。
    - [ ] **Work Item（Deferred）**: 本阶段不引入 `brief_go` 数据库角色，待 `briefs` 重启时再评估。
    - [ ] **Work Item（Deferred）**: 本阶段不为 `brief_go` 增加 StrategyFactory 显示名称映射。
    - [ ] **Work Item（Deferred）**: 本阶段不增加 `brief_generator` 的 Go 单次验证 CLI。

### 2.1 前端分层与发放口径（v1 已落地）
- [x] **Tier 扩展**：前端用户层级由 `free/pro` 扩展为 `free/go/plus/pro/alpha`（含类型与缓存口径）。
- [x] **预测路由（v1 口径）**：
  - `free` 仅混元；
  - `go` 仅 DeepSeek；
  - `plus` 在 Go 基础上增加 Gemini；
  - `free/go/plus` 不再使用 `rule-engine`。
- [x] **InvestmentMode 可见性**：`free/go/plus` 隐藏入口，仅 `pro/alpha` 显示。
- [x] **邀请与引导体验口径**：邀请通过与 onboarding 后默认体验调整为 `go`（不再默认发 `pro`）。
- [x] **个人中心文案同步**：会员身份与入口文案按新 tier 显示（如 `Go 会员`、`解锁 Go 权益`）。
- [x] **日报入口分层**：`free/go/plus` 移除右上角“日报”入口，仅 `pro/alpha` 保留。
- [x] **黄历能力分层（展示 + 请求）**：
  - `free/go/plus` 不展示“大盘黄历”；
  - 前端在对应 tier 下不再发起黄历数据请求（并停止无效缓存写入）；
  - 导出面板中 `free/go/plus` 不展示“个股黄历”。
- [x] **Dashboard Vercel Phase 2（v1 发布前收口）**：
  - `/api/stock/batch` 明确收敛为单路由 `tier-gated public facts`，内部拆分为 facts 组装与 tier 投影视图；
  - 新增轻量 `/api/user/bootstrap`，收口 Dashboard 入口私有初始化请求；
  - 保留本地 snapshot 秒开主链路，不将 dashboard 主数据吞入 bootstrap。

### 3. 数据：信任勋章与全局路由 (Trust & Routing)
- [x] **勋章 UI 实现**：根据 `max_perf_in_window` 与 `validation_status` 在卡片上渲染 `Verified` 勋章。
- [x] **Time Machine 逻辑确认**：确保历史信号快照的只读一致性。
- [x] **市场路由全局化**：在 `market_routing.py` 中新增 `US` 市场元数据项（yfinance 适配）。

---

## 🟨 P1: 网站国际化翻转 (i18n Flip — English-first, Multi-locale)

> **架构决策**：
> - 主站切换为英文 (`/`)，韩语 (`/ko/`)、西班牙语 (`/es/`) 为国际化语言扩展
> - 中文使用 `/cn/` 子目录，定位为 **相对独立的中国版**
> - 选用 `/cn/` 而非 `/zh/`，为未来"国际版 vs 中国版"的独立产品线预留架构空间
>
> **语言矩阵**：
> | 路由 | 语言 | hreflang | 定位 |
> |:---|:---|:---|:---|
> | `/` | 🇺🇸 English | `en` | 国际主站 (x-default) |
> | `/ko/` | 🇰🇷 Korean | `ko` | 高净值跨境散户 (精度) |
> | `/es/` | 🇪🇸 Spanish | `es` | 拉美+西语裔散户 (广度) |
> | `/cn/` | 🇨🇳 Chinese | `zh-CN` | 中国版 (独立版本线) |

### Phase 1: 基础设施翻转 (Infrastructure Flip)

#### Story 1: 核心配置翻转
- [x] `public-i18n.ts`: `DEFAULT_PUBLIC_LOCALE` 从 `'zh'` → `'en'`
- [x] `SUPPORTED_PUBLIC_LOCALES` 从 `['zh', 'en']` → `['en', 'ko', 'es', 'cn']`
- [x] `INDEXABLE_PUBLIC_LOCALES` 从 `['zh']` → `['en', 'ko', 'es', 'cn']`
- [x] `localizePublicPath`: `en` 无前缀，`ko`/`es`/`cn` 加各自前缀
- [x] `getLocaleHrefLang`: `en` → `'en'`，`ko` → `'ko'`，`es` → `'es'`，`cn` → `'zh-CN'`

#### Story 2: Middleware 迁移
- [x] 无前缀 public 请求标记为 `en`（当前标记为 `zh`）
- [x] `/ko/*`、`/es/*`、`/cn/*` 前缀识别与 locale 注入
- [x] `content-language` header 按 locale 动态设置
- [x] `app.ziso.cc` 保护逻辑不变（已审计通过，实站访问验证正常）

#### Story 3: SEO 301 迁移保护
- [x] `/en` → `/` (301)，`/en/about` → `/about` (301) 等旧英文路由全量 301
- [x] `x-default` 指向英文根路径
- [x] Sitemap 扩展为四语言，英文 `priority: 1.0`，其余 `0.7`
- [x] 一级营销页与法律页输出四语言 `hreflang` alternates
- [x] 内容页 `hreflang` 策略补齐：按最终信息架构明确 `/learn`、`/support`、`/cn/learn`、`/cn/support` 的 alternates 关系
- [x] **SSG 最终验证**：确认 CDN 节点可直接分发 HTML，首屏 TTFB 降至毫秒级。

#### Story 3.5: Public Site 静态化恢复 (Completed ✅)
- [x] **移除根布局请求头依赖**：`app/layout.tsx` 不再通过 `headers()` 读取 `x-ziso-locale`，避免整站 public 页面被强制动态渲染
- [x] **按路由段恢复静态语言输出**：将 `html lang` 的服务端输出下沉到可静态化的 public 路由层，保留 `en/ko/es/cn` 正确语义且不依赖 request-time API
- [x] **为 public locale 路由补 `generateStaticParams()`**：覆盖 `/ko|/es|/cn` 的 home/about/pricing/privacy/terms/refund，以及最终确定的信息页路径
- [x] **验证 public 页面重新回到 SSG**：`next build` 中 `/`、`/about`、`/pricing`、`/privacy`、`/[locale]/[[...slug]]` 不再显示为 `ƒ` 动态页
- [x] **评估 public 页 edge 分发方案**：确认“静态产物 + CDN/Edge 分发”即可满足 SEO 与成本目标，避免为 marketing 页引入不必要的 server runtime
- [x] **补充上线门禁**：增加一条 release check，防止未来再次因 `headers()` / `cookies()` / 动态 API 把 public site 拉回动态渲染

### Phase 2: 页面与组件翻转 (Page Swap)

#### Story 4: 路由结构反转
- [x] 拆解 `EnglishPublicPages.tsx` (1185 行) 为独立页面，替换根路由 `page.tsx` 系列
- [x] 封装中文营销页组件集，承载当前中文首页/About/Pricing 等内容
- [x] `[locale]/[[...slug]]/page.tsx` 从服务 `en` 转为服务 `ko`/`es`/`cn`

#### Story 5: 导航与语言切换器
- [x] `MarketingHeader.tsx`: 四语言切换器（EN / 한국어 / ES / 中）
- [x] `MarketingFooter.tsx`: 同步翻转 footer 语言链接
- [ ] 语言切换器 UI 评估是否升级为下拉选择器（当前按钮组可用，非发布阻断）

### Phase 3: 内容策略 (Content Strategy)

> **策略**：国际站 (en/ko/es) 一级页面多语言，二级内容页 (Learn/Support) 仅英文。
> CN 站 (`/cn/`) 保持中文内容独立运营。

#### Story 6: 国际站内容架构 (Completed ✅)
- [x] 根 `/learn/*` 和 `/support/*` 展示英文内容（提供高质量 Hero 翻译版）
- [x] `/ko/learn` 和 `/es/learn` 不创建独立内容，重定向至根 `/learn`（英文）
- [x] `/ko/support` 和 `/es/support` 同理，重定向至根 `/support`（英文）
- [x] 内容加载器 `learn-content.ts` / `support-content.ts` 默认加载英文内容

#### Story 6.5: CN 站中文内容 (Completed ✅)
- [x] `/cn/learn/*` 承载中文 101 Academy 内容
- [x] `/cn/support/*` 承载中文帮助中心内容
- [x] 内容加载器按 `cn` locale 加载中文 Markdown

### Phase 4: 韩语/西语一级页面 (KO/ES L1 Pages)

#### [x] Story 7: 韩语营销页面 (KO L1 Pages) <!-- id: 7 -->
- [x] `/ko/` 首页、About、Pricing 韩语版本
- [x] `/ko/privacy`、`/ko/terms`、`/ko/refund` 法律页韩语版本

#### [x] Story 8: 西班牙语营销页面 (ES L1 Pages) <!-- id: 8 -->
- [x] `/es/` 首页、About、Pricing 西语版本
- [x] `/es/privacy`、`/es/terms`、`/es/refund` 法律页 西语版本

### 审核记录（2026-04-02）

- [x] **SSG 目标达成**：`npm run build` 验证通过，整站 Public 路由显示为 `○` (Static) 或 `●` (SSG)，彻底告别动态渲染带来的性能缩减。
- [x] **国际站翻转完成**：根路径已切换为英文首页/Academy，`/cn/` 成功剥离为独立运行的中文版。
- [x] **Hero 内容上线**：首批英文文章（101-01, 101-99, 101-100, Syllabus）已进入生产环境，符合 "Hero-First" 翻译质量标准。
- [x] **类型与兼容性**：修复了 Next.js 15 的 params Promise 兼容性问题与内容加载器的命名碰撞。

---

## 🟧 P1.5: 全球市场接入 (Market Scope)

### 7. AI 预测英文化 (AI Prediction i18n — Signals & Reasoning)
- [x] **提示词模板双语化**：完成 `stock_analysis_system_b2.j2` 和 `stock_analysis_user_b2.j2` 的条件分支逻辑（根据 `locale` 输出英/中）；系统模板尾部「输入说明」英文化。
- [x] **推理逻辑透传**：`prepare_stock_analysis_prompt(..., locale=)` 与用户模板数据项（市场/海拔/长期趋势等）按语言输出；`--locale en` / `PREDICTION_CONTENT_LOCALE` 贯穿 `PredictionRunner`；`ai_predictions_v2.content_locale` 记录生成语言。
- [x] **多模型适配**：OpenAI 兼容路径（含 DeepSeek Aliyun）与混元链（`anchor`/`analysis`/`synthesis` 步骤）在 `locale=en` 下英文提示；`gemini_local` 透传 `locale`。

### 7.1 [DEFERRED] 推理报告英文化 (Reasoning/Brief i18n)
> ⚠️ Daily Brief 功能在 v1.0.0 (Free/Go/Plus/Pro) 中暂不开放，相关国际化工作整体后置。
- [ ] **模板策略升级**：扩展 `brief_strategies.py` 以透传 `locale` 参数。
- [ ] **Jinja2 模板重写**：完成 `prompts/briefs/system_*.j2` 的双语条件分支逻辑。

### 8. 美股数据接入 (US Market ETL)
- [x] **美股路由配置**：在 `market_routing.py` 中新增 `US` 市场元数据。（v1.0.0 已完成）
- [ ] **yfinance 接入**：开启美股核心标的的 ETL 分析排期。

---

## 🟪 P1.8: Dashboard 应用 UI 国际化 (App i18n Architecture)

> **现状审计**：Dashboard 侧共有 **~12,600 行** 包含中文硬编码，分布在 dashboard 组件 (3,908)、lib 逻辑层 (5,076)、共享组件 (~2,700)、路由页 (983)。
> 中文不仅存在于 UI 文案，还深入到了 **语义数据层** (`semantic-registry.ts` 中 `'建议看多'` 等是数据库存储值)。
> 因此应用 i18n 不是简单文案替换，而是"文案层 + 语义映射层"的双轨工程。

### Story 7: i18n 文案抽离架构 (Message Extraction Architecture)
- [x] **选型决策**：确认方案 — 自研 `messages/*.json` + `useT()` hook（零依赖、ISR 兼容）
- [x] **建立 `messages/en.json` 与 `messages/cn.json` 骨架**：7 namespace（`common`, `dashboard`, `brief`, `council`, `user`, `onboarding`, `trade`），~80 key
- [x] **工具函数封装**：`useT(namespace)` hook + `useGlobalT()` + `useLocale()`，类型安全（编译期 key 拼写检查）
- [x] **`app.ziso.cc` 语言识别**：localStorage → profile locale → navigator.language → 默认 `cn`，不走 URL 前缀
- [x] **质量门**：`tests/i18n-integrity.test.mjs`（结构同构 + 空值 + 占位符一致性），集成 `test:quality`

### Story 8: 语义层双轨化 (Semantic Layer Bilingual)
- [x] **`semantic-registry.ts` 重构**：保持数据库存储值为中文（`'建议看多'`），前端增加 `semanticToI18nKey` 映射表
- [x] **`layer1-ui.ts` 拆分**：`headline`/`badge`/`posterDecision` 从硬编码中文改为 `i18n key` + locale 查表
- [x] **`historical-card-surface.ts` 适配**：勋章标签 (`验证通过`/`金牌验证`) 支持 locale 输出（`Verified +X%` / `Gold +X%`）
- [x] **`date-utils.ts` 适配**：日期标签 (`今日建议`/`明日预判`) 支持 locale

### Story 9: Dashboard 核心组件文案迁移 (Component Migration)
> 按中文密度降序迁移，每个组件独立可交付

- [x] **TacticalBriefDrawer.tsx** (862 行) — 研判报告全量文案
- [x] **TacticalReportPoster.tsx** (383 行) — 海报导出文案
- [x] **StockDashboardCard.tsx** (263 行) — 主卡片信号文案
- [x] **HistoricalCard.tsx** (152 行) — 历史回看文案

### Story 10: 用户体验层文案迁移 (UX Layer Migration)
- [x] **UserCenterDrawer.tsx** (608 行) — 个人中心 (含 Identity, InvestmentMode, Support, Learn 子视图)
- [x] **OnboardingOverlay.tsx** (384 行) — 新用户引导 (已完成)
- [x] **UserPricingView.tsx** (232 行) — 订阅与定价 (已完成)
- [x] **InstallGuide.tsx** (218 行) — PWA 安装引导 (已完成)
- [x] **SettingsModal.tsx** (131 行) — 设置弹窗 (已完成)

### Story 10.5: 证券展示名数据层国际化 (`stock_meta.name_en`) (Completed ✅)

> 英文界面下证券名以 **数据库 `name_en`** 为准；`backend/data/*_name_en_curated.json` 仅 **`sync_meta` 写库**，运行时 **不读 JSON**。详见 `docs/1_Engineering/i18n/01_Stock_Name_Internationalization.md` §15–§16。

- [x] **Schema / 写入安全**：`stock_meta.name_en`、`add_column_if_missing`、安全 UPSERT（不抹 `industry` 等）
- [x] **ETL**：港股 Sina 英文列 + `sanitize_hk_name_en_candidate`；CN/HK 运维策展 JSON 在 bulk upsert 后覆盖写库
- [x] **API**：`/api/stock-pool`、`/api/stock/search`、`/api/user/onboarding/stocks`（及 dashboard 相关）返回 `name_en`
- [x] **前端**：`getLocalizedStockName`；自选 `watchlistMetaDiffers` + `addStock(..., name_en)`；Dashboard 用自选覆盖缓存中的 `name`/`name_en`；搜索 SQL 占位符与参数个数一致
- [x] **路由**：`middleware` 区分 `ziso.cc`（`/dashboard` → `app.ziso.cc`）与 `localhost` 本地开发
- [x] **本地验收**：`npm run verify:local-stock-name-en`；质量门含 `/dashboard` 与营销域重定向用例

**仍属后续（非阻断）**：全仓库脚本/报表对「仅 `name`」假设的审计（对应设计文档 §15.5 / §15.1 P1 末条）。

---

## 🟩 P1.9: 国际定价 GTM 与环境接力 (International Pricing & Persistence)

> **核心挑战**：如何确保在官网 (`ziso.cc`) 选择了韩语/西语的用户，进入 App (`app.ziso.cc`) 后依然能看到对应的币种和定价，而非跳回默认的中文/人民币。这就是国际化架构中“最后的一环”。

### Story 11: 动态定价 ID 引擎 (Locale-Aware Pricing Engine)
- [ ] **`usePricingData` Hook 实现**：重构 `pricing-data.ts`，由静态数组转为按 `locale` 动态返回 `priceId` 与 `price` 字符串。
- [ ] **双轨 ID 映射**：
    - `cn` -> `PRO_MONTHLY` (¥29.9) / `PRO_YEARLY` (¥299)
    - `en/ko/es` -> `GO_MONTHLY` ($4.99) / `GO_YEARLY` ($49.9)
- [ ] **App 内部感知**：`UserPricingView.tsx` 动态适配，海外用户在 Dashboard 内部直接看到 USD 支付入口。

### Story 12: 跨域环境接力 (Cross-Domain Locale Persistence)
- [ ] **Cookie 埋点 (Middleware)**：在 `ziso.cc` 命中路径语言前缀时，写入 `.ziso.cc` 作用域的 `ziso_locale` Cookie。
- [ ] **App 环境恢复**：`i18n.ts` 的 `resolveLocale` 逻辑补齐对 `ziso_locale` Cookie 的优先级读取，确保官网到 App 的“无感跳转”。
- [ ] **App 内部同步**：用户在 App 切换语言时，同步更新该跨域 Cookie，确保跳回官网时语言不重置。

### Story 13: 营销与支付链接闭环 (Marketing-to-Checkout Bridge)
- [ ] **Landing Page 按钮激活**：更新 [EN/ES/KO]PricingPage 按钮，通过 `?priceId=...` 直接导向 App 定价中心。
- [ ] **App 自动导流 (Auto-Checkout)**：App 定价页检测到 `priceId` 时，若用户已登录，自动调起 Stripe 支付会话。

---

## 🟦 P2: 增长与内容自动化 (Growth & Ops)

### 6. 内容生产自动化 (Ops)
- [ ] **Content Integrity (v1 Zero-Debt)**: 101/Support 资产通过 `content-audit` 审计，红区/橙区告警清零。
- [ ] **Bilingual Mirroring (v1 i18n)**: 101 学院 Hero 文章 (101-01 to 101-20) 完成 EN/CN 镜像对齐。
- [ ] **Visual Registry (v1 Assets)**: 首批 50 篇文章 `visual_assets.cover.status` 达到 `approved`。
- [ ] **Blitz-to-Hub 自动分流**: 建立脚本自动筛选优胜案例并推送至 Content Queue。
- [ ] **101 学院 i18n**：核心文章翻译定稿（VCP/止损/风险）。
- [ ] **大师系列对焦**：确保 ms-01 叙事点与 DeepSeek 识别逻辑 1:1 挂钩。


---

## 🛠️ 核心文件锚点 (Physical File Anchors)
*   **前端交互**：`frontend/src/components/dashboard/TacticalBriefDrawer.tsx`
*   **引擎路由**：`backend/engine/llm_registry.py`
*   **研判策略**：`backend/engine/models/brief_strategies.py`
*   **提示词模板**：`backend/prompts/briefs/system_*.j2`
*   **市场配置**：`backend/management/research/market_routing.py`

> 功能不包含在 v1 free、go、plus、pro 版本中，低优先级，不启动实际的实施工作，仅记录
- [ ] **Daily Brief** (daily_briefs 表) — 每日深度简报 (v1.0 暂不开放)
- [ ] **AICouncil.tsx** (163 行) — 投研决议文案
- [ ] **MarketAlmanacFeed.tsx** (345 行) — 黄历文案
- [ ] **TradeManagementTab.tsx** (340 行) — 交易管理文案
