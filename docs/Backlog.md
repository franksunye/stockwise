# 知守 AI (ZISO AI) 项目待办清单 (Backlog)

> **小步快跑** —— 将大愿景拆解为可落地的任务。

---

## 🔥 当前最高优先级 (Sprint 10: 双层架构止血与稳态推进)

### 0. 双层架构 #1 止血修复（P0，已完成）
- [x] **修复 Side 判定绝对值漏洞**：`backend/engine/validator.py` 已改为 `abs(cumulative_change) <= NOISE_THRESHOLD`，避免暴跌样本误标 `Correct`。
- [x] **解除硬编码一刀切降级**：`backend/engine/ai_service.py` 已将风控熔断改为可配置模式（`warn` / `force_side` / `off`），默认 `warn` 保留方向信号。
- [x] **补齐回归测试**：新增 `backend/tests/test_ai_service_guardrail_modes.py`，覆盖三种 guardrail 模式。
- [x] **代码已合入主干**：提交 `ef89bd1` 已推送 `origin/main`。
- [x] **本地验证闭环（SQLite）**：`$env:DB_SOURCE='local'; python backend/main.py --verify --force` 通过，`Validation Complete: 495 predictions updated`。
- [ ] **生产验证补齐（运维）**：在 GitHub Actions 触发 `verify_predictions.yml` 并留存 run 链接与结果截图（当前受本地研发策略切换影响，待运维窗口执行）。

### 0.1 双层架构 #2 稳妥推进（P0，本周）
- [x] **Layer-1 状态机最小闭环**：已落地 `NoSetup/Watch/TriggeredLong/RiskOff` 的计算与存储字段，不改前端展示。
- [x] **参数治理收口**：阈值已统一收敛到 `backend/strategy_config/tradeability_params_v1.json`，移除散落硬编码。
- [x] **验收标准（阶段一）**：本地 SQLite 已完成可复现验证，状态机输出与参数文件可追踪。

### 0.1.1 双层架构 #2 后续参数迭代（P0，进行中）
- [ ] **覆盖率提升目标**：将 `TriggeredLong` 覆盖从当前约 `1.51%`（在线口径回放）提升到验收区间 `5%~20%`。
- [ ] **单参数迭代规则**：每轮仅调整 1 个参数（优先 `breakout_volume_mult`、`momentum_change_threshold`），避免多变量混淆。
- [ ] **风险护栏**：每轮必须同时复核 `RiskOff` 占比、最大回撤、方向一致率，不允许仅追求 Long 覆盖。
- [ ] **周度汇报**：通过 `acceptance_weekly` 输出 gate 状态并同步 webhook 结论。

### 0.1.2 Investment Mode 绩效口径收口（P0，待排期）
- [ ] **前端/API 双绩效展示**：Investment Mode 前端与 API 需要拆分 `research_performance` 与 `production_performance`，默认优先展示模式发布物自带的研究绩效。
- [ ] **口径文案统一**：用户侧明确区分“研究表现”和“你的自选池表现/正式表现”，避免将研究样本结果误读为个人实际效果。
- [ ] **验收标准**：模式页可同时读取并展示两类绩效；研究绩效来自 `mode_params_bundles_v1.json.market_release_artifacts.*`，生产绩效继续来自 `mode_performance_snapshot`。

### 0.2 双层架构 #3 编排改造（P1）
- [ ] **Runner 流程改造**：先算 Layer-1，再将状态注入 LLM 上下文，替代并行盲猜。
- [ ] **Prompt 职责降维**：方向由量化定，LLM 只负责战术解释与风险沟通。
- [ ] **验收标准**：A/B 对照下，方向一致性与解释稳定性提升且不回归性能。

---

## 🔥 当前最高优先级 (Sprint 9: 执行团队产品化与系统露出)

### 0. 执行团队拟人化露出收口（P0）
- [ ] **统一命名与角色分层**：全站统一使用成员中文名与职级（如资深/初级/规则分析师），不再混用模型名作为前台主展示。
- [ ] **个股简报底部署名分层**：PRO 使用“顾深”，Free 使用“林序”，并统一头像样式与职级文案。
- [ ] **投研决议卡片统一**：成员名前统一使用头像（与官网一致），移除括号模型别名展示，保留既有信息密度不新增“分歧来源”等字段。
- [ ] **验收标准**：抽样首页、关于、个股简报、投研决议 4 个入口，成员命名/头像/职级 100% 一致。

### 0.1 团队职责覆盖与精简治理（P0）
- [ ] **职责矩阵冻结**：以“顾深/林序/程矩三位分析师 + 沈策量化工程师 + 诺岚情报补充 + 维尔结果复核 + 安德烈兼任研究负责人”为当前标准版本。
- [ ] **一人多岗规范化表达**：对兼任职责采用“主责/协同”字段，避免职责冲突与口径不一致。
- [ ] **文案口径统一**：对外统一使用“团队/执行团队/研究团队”等中文表述，不使用“Agent Team”。
- [ ] **验收标准**：站内所有团队介绍文案无英文组织名残留，职责描述可映射到矩阵且无空白功能位。

### 0.2 结论呈现密度治理（P1，本周）
- [ ] **证据锚点入口**：为每条结论增加“证据锚点”入口，统一支持技术位 / 新闻位 / 规则位三类来源跳转或定位。
- [ ] **分歧展示轻量化**：分歧区不新增信息密度，仅保留“分歧/一致”状态与“成员结论标签”。
- [ ] **验收标准**：抽样个股简报与 投研决议页面，用户可在 1 次点击内到达对应证据锚点；分歧区信息量与当前版本持平。

### 0.3 专项不遗漏清单（执行对勾）（P1，本周）
- [ ] 首页/关于：成员中文名、职级、职责矩阵口径一致。
- [ ] 个股简报：PRO=顾深，Free=林序，且底部署名头像与官网一致。
- [ ] 投研决议：成员名前使用头像；不显示括号模型别名。
- [ ] 分歧模块：仅保留“分歧/一致 + 成员结论标签”，不新增信息字段。
- [ ] 结论证据：每条结论可定位到技术位/新闻位/规则位锚点。
- [ ] 官网流程图：下周上线“3轨独立研判 -> 比对 -> 风险提示”。

---

## 🔥 当前最高优先级 (Sprint 8: 系统稳定性与数据质量)

### 0. 周期数据可靠性升级（World-Class Gap Closure）(P0)
- [ ] **移除周/月静默吞错**：`run_full_sync()` 中 weekly/monthly 的 `except: pass` 改为结构化错误上报 + 重试队列。
- [ ] **结构化重试策略**：为周期同步引入分级重试（瞬时网络错误重试、逻辑错误快速失败），并记录最终失败原因。
- [ ] **验收标准**：连续 7 天任务运行中，weekly/monthly 失败任务可在日志与任务看板中 100% 可追踪（symbol、period、error_type、retry_count）。

### 0.1 数据质量 SLO 与告警 (P0)
- [ ] **定义 SLO 指标**：污染率、空表率、周期覆盖率、当日同步成功率。
- [ ] **自动告警**：当任一指标超过阈值（如空表率 > 2%，污染率 > 0）自动推送告警。
- [ ] **验收标准**：任一指标越线后 5 分钟内触发告警，并附带可执行修复建议（脚本/命令）。

### 0.2 周期重建能力收敛 (P1)
- [ ] **统一库函数**：将“日线重建周/月”的逻辑收敛为单一模块，供同步主流程、prompt 兜底、修复脚本共享。
- [ ] **消除重复实现**：减少 `sync/prices.py`、`engine/prompts.py`、`scripts/repair_periodic_data_v3.py` 的重复聚合逻辑。
- [ ] **验收标准**：周期聚合核心逻辑单点维护；改动一次可覆盖 3 条链路（同步/读取/修复）。

### 0.3 端到端回归链路 (P1)
- [ ] **E2E 回归用例**：覆盖 `同步 -> 入库 -> prompt -> 预测` 全链路（至少 3 个代表标的：CN/HK/指数）。
- [ ] **故障注入测试**：模拟上游接口失败，验证 fallback 与告警是否按预期触发。
- [ ] **验收标准**：每次发布前自动执行，关键链路回归失败可阻断发布。

### 0.4 战术契约工业化收口 (P0)
- [ ] **关键位来源可观测化**：将 `key_levels_meta`（`model/derived/fallback`）落库或埋点上报，形成日级统计。
- [ ] **线上质量看板**：新增“策略契约合规率”与“关键位 fallback 占比”趋势图，按模型维度拆分（DeepSeek / Gemini / Rule）。
- [ ] **发布门禁**：上线前跑生产镜像回放，要求 `tactics` 合规率 100%、`immediate_support/resistance` 长度=2 合规率 100%。
- [ ] **告警降噪策略**：质量告警从“每窗口报警”升级为“连续 N 窗口低于阈值再报警”，减少噪声告警。
- [ ] **验收标准**：连续 7 天可追踪并可解释所有 fallback 来源，且异常可在 5 分钟内定位到模型与样本。

### 0.5 语义可信度增强 (P1)
- [ ] **前端来源提示**：当关键位来源为 `derived/fallback` 时显示“参考位”语义标签，避免用户误读为模型强结论。
- [ ] **策略质量评分**：新增“主/次策略可区分度”评分（action+trigger 去重之外，补充语义相似度阈值）。
- [ ] **低质量降级**：当次策略质量低于阈值时，自动替换为标准观察预案并记录事件。
- [ ] **验收标准**：抽样 100 条 Pro 结果，策略重复/伪差异率低于 2%。

### 1. 新股数据质量检查 (P0)
- [ ] **数据完整性验证**：在 `process_stock_period()` 中添加数据行数检查
- [ ] **跳过 AI 预测逻辑**：数据不足 30 天时仅存储价格数据，不生成 AI 预测
- [ ] **前端标识**：在 UI 中标注"数据不足"的股票

### 2. 任务超时保护 (P1)
- [ ] **GitHub Actions 超时配置**：为 `daily_sync.yml` 添加 `timeout-minutes: 45`
- [ ] **AI 调用超时控制**：为 AI 预测添加 30 秒超时 + 降级策略
- [ ] **数据库写入重试**：添加 exponential backoff 重试机制

### 3. 错误处理增强 (P2)
- [ ] **结构化错误日志**：改进错误详情记录（不截断为 100 字符）
- [ ] **错误类型分类**：区分数据源错误、计算错误、网络错误
- [ ] **详细错误报告**：企业微信通知包含错误类型和时间戳

### 4. 多周期同步依赖优化 (P2)
- [ ] **依赖关系检查**：日线失败时跳过周线/月线
- [ ] **返回值标准化**：`process_stock_period()` 返回 success/failure 状态
- [ ] **事务一致性**：确保多周期数据的原子性

---

## 🏃 迭代计划 (Next Sprints)

### Sprint: 数据质量监控 (P3)
- [ ] **创建 `data_quality_metrics` 表**：记录每次同步的数据完整性指标
- [ ] **AI 预测准确率追踪**：按时间维度统计胜率趋势
- [ ] **数据源可用性监控**：记录 AkShare API 的成功率
- [ ] **质量报告仪表板**：可视化数据质量指标

### Sprint: 停牌/退市股票管理 (P3)
- [ ] **添加股票状态字段**：`status: active/suspended/delisted`
- [ ] **自动状态检测**：连续 N 天无数据更新 → 标记为 suspended
- [ ] **ETL 跳过逻辑**：不同步非活跃股票
- [ ] **定期清理任务**：移除长期退市的股票

### Sprint: 核心股票池优化
- [ ] 筛选 50 只高流动性标的列表
- [ ] 优化 ETL 效率，支持批处理同步

### Sprint: 用户体验进阶 (Discover & Radar)
- [ ] **Stock Radar 核心引擎 (P1)**：开发支持“指标 + AI 信心”的复合筛选 API。
- [ ] **策略预设系统 (P1)**：内置“低价创业板”、“技术面突破”等官方雷达预设。
- [ ] **前端发现中心 (P2)**：设计并实现 Web 端/App 端的智能筛选与策略管理 UI。
- [ ] **雷达监控自动监测 (P2)**：实现后台异步扫描并触发“信号翻转”通知。
- [ ] PWA 离线访问支持
- [ ] 多策略对比：支持对比不同的 AI 分析模型

### Sprint: 前端架构深层进化 (SWR Architecture, P1)
- [ ] **投研决议换擎**：将 `AICouncil.tsx` 的手写内存 `Map` 与挂载检查重构为 `swr` 声明式调用。
- [ ] **自选池防幻影迭代**：使用 SWR 自带的 `mutate(optimisticData)` 改写 `useWatchlist.ts` 的本地乐观更新机制与同步守护期（Anti-Zombie）。
- [ ] **大盘主线清理**：剥离 `useDashboardData.ts` 中的 `visibilitychange`、复杂缓存与定时器，利用 `refreshInterval` 统一接管 CDN 行情轮询。
- [ ] **收益与测试**：全量替换后核验打包体积与 API 重复调用率是否清零，确保组件热加载与渲染体验与现况持平及以上。

### Sprint: 团队机制可视化（下周，P2）
- [ ] **官网流程图模块**：新增“团队如何做结论”可视化流程图（3 轨独立研判 -> 比对 -> 风险提示）。
- [ ] **页面落位与样式统一**：流程图在官网首页/关于页采用一致视觉语言与术语口径。
- [ ] **验收标准**：流程图可在移动端与桌面端清晰展示，且与团队职责矩阵描述一致。

---

## 📅 长期愿景 (Icebox)
- [ ] 接入更多维度数据 (新闻舆情、龙虎榜数据)
- [ ] 实现全站/单股的胜率排行
- [ ] 支持用户自定义 AI 角色（如：激进型、稳健型风控官）
- [ ] 多市场支持（美股、加密货币）
- [ ] 社区功能（用户分享、评论）

---

## 📝 开发指南
1. **先验证，后预测**：ETL 运行时，必须先判定昨日预测的死活，再生成今日的新建议。
2. **数据为王**：所有 AI 的文字分析都必须有对应的数值支撑。
3. **零容忍幻觉**：如果指标缺失，AI 必须返回 "Side/观望" 信号。
4. **边界情况优先**：新股、停牌、退市等边界情况需要特殊处理。
5. **失败即通知**：所有后台任务失败都应发送通知，不能静默失败。

---

**最后更新**: 2026-03-10
