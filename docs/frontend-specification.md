# StockWise 前端开发规格 v1.0

> **Mobile-First PWA** —— 即使情绪崩溃，也强制执行纪律

---

## 🛠️ 技术栈

| 类别 | 选型 |
|------|------|
| 框架 | Next.js 14+ (App Router) |
| 语言 | TypeScript |
| 样式 | Tailwind CSS |
| 图标 | Lucide React |
| 服务端状态 | React Server Components → Turso |
| 本地状态 | localStorage |

---

## 🎨 设计系统

### 配色方案 (Dark Mode)

| 用途 | Tailwind Class | 色值 |
|------|----------------|------|
| 全局背景 | `bg-slate-950` | #020617 |
| 卡片背景 | `bg-slate-900` | - |
| 主要文字 | `text-slate-50` | - |
| 次要文字 | `text-slate-400` | - |
| 看多/盈利 | `text-emerald-500` | 🟢 |
| 止损/亏损 | `text-rose-500` | 🔴 |
| 观望/震荡 | `text-amber-400` | 🟡 |

### 字体

- **数字**：等宽字体 (`Geist Mono` / `Roboto Mono`)
- **中文**：系统默认 (`PingFang SC` / `Microsoft YaHei`)

---

## 📄 页面规格

### 1. 决策看板 (Dashboard)

**路径**：`/` 或 `/dashboard`

#### 布局结构

```
┌─────────────────────────────────┐
│  ☰    [股票选择器]         ⚙️   │  ← Header
├─────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐       │
│  │  现价   │  │ 关键位  │       │  ← 2x2 HUD Cards
│  │  涨跌幅 │  │  状态   │       │
│  └─────────┘  └─────────┘       │
│  ┌─────────┐  ┌─────────┐       │
│  │  成交量 │  │   RSI   │       │
│  └─────────┘  └─────────┘       │
├─────────────────────────────────┤
│       [ AI 决策结论区 ]         │  ← The Verdict
│    "今日建议：观望"             │
│    "缩量回调，未触及买入点"     │
├─────────────────────────────────┤
│                           📷    │  ← FAB 拍照分析
└─────────────────────────────────┘
```

#### 信号逻辑

| 条件 | 显示 |
|------|------|
| `price < support` | ⚠️ **跌破支撑** (红底，呼吸动画) |
| `price > pressure` | 🚀 **突破压力** (绿底) |
| 其他 | 震荡中 |

---

### 2. 风控设置 (Settings Modal)

**形式**：Bottom Sheet / Modal

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 目标股票 | 只读 | - | 当前代码 |
| 🔴 止损/支撑位 | number | ✅ | 跌破无条件离场 |
| 🟢 突破/压力位 | number | ❌ | 突破且放量才试错 |

**存储**：`localStorage.stock_rules`

---

### 3. 历史复盘 (History)

**路径**：`/history`

#### 统计看板

| 指标 | 示例 |
|------|------|
| 策略胜率 | 40% |
| 累计避险/收益 | +12.5% ✨ |

#### 时间轴结果计算

| 场景 | 显示 |
|------|------|
| 建议卖出 → 次日跌 | ✅ **避险 5%** (绿) |
| 建议买入 → 次日跌 | ❌ **浮亏 2%** (红) |
| 建议观望 → 次日涨 | ⚪ **踏空 3%** (灰) |

---

## 📊 数据接口

### Turso 表结构 (Server Component 读取)

```typescript
interface DailyPrice {
  symbol: string;         // '02171'
  date: string;           // '2025-12-20'
  close: number;          // 15.0
  change_percent: number; // 1.28
  volume_ratio: number;   // 0.44
  rsi_14: number;         // 43.2
  ma_20: number;          // 15.1
  ai_summary: string;     // "缩量震荡，建议观望"
}
```

### LocalStorage 结构

```typescript
interface UserRules {
  [symbol: string]: {
    support_price: number | null;
    pressure_price: number | null;
    last_updated: number;
  }
}
```

---

## 🚀 开发清单 (Phase 1)

| # | 任务 | 说明 |
|---|------|------|
| 1 | Setup | Next.js 初始化 + Tailwind + Turso |
| 2 | Components | `StockCard`, `SignalBadge`, `BottomNav` |
| 3 | Dashboard | Turso 数据 + LocalStorage 规则对比 → 红绿灯 |
| 4 | Settings | 规则录入弹窗 |
| 5 | History | 时间轴 + Mock 避险收益 |
| 6 | PWA | `manifest.json` + 全屏支持 |

---

## ⚠️ 异常处理

| 场景 | 处理 |
|------|------|
| 无数据/休市 | 显示最近交易日数据 |
| 未设置规则 | 引导卡片 → "点击设置风控锚点" |
| 网络错误 | Toast 提示 + 下拉刷新 |

---

*文档版本: v1.0 | 更新日期: 2024-12-22*
