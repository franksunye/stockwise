# Dashboard 显示逻辑规格

> 版本: 1.0  
> 创建时间: 2025-12-26  
> 状态: 已确认

---

## 一、核心原则

**以用户行动为中心**：用户打开界面时，最关心的问题是"接下来该怎么做"，而不是"昨天预测对不对"。

### 设计理念

1. **极简**：只显示当前最具行动价值的信息
2. **直白**：普通用户秒懂，无需金融术语
3. **聚焦**：开市时聚焦"今天"，收市后切换到"明天"

---

## 二、时间场景定义

基于港股交易时间划分三个场景：

| 场景ID | 名称   | 时间段 (港股) | 用户心态                       |
| ------ | ------ | ------------- | ------------------------------ |
| **S1** | 开市前 | 00:00 - 09:30 | "今天要盯盘了，我该怎么做？"   |
| **S2** | 交易中 | 09:30 - 16:00 | "盘中对比预测和走势，决定行动" |
| **S3** | 收市后 | 16:00 - 24:00 | "今天结束了，明天怎么安排？"   |

> 注：午休 12:00-13:00 归类为 S2（交易中），因用户仍在关注当日走势。

---

## 三、界面元素显示规则

### 3.1 显示矩阵

| 界面元素          | S1 开市前      | S2 交易中    | S3 收市后    |
| ----------------- | -------------- | ------------ | ------------ |
| **主标题**        | "今日建议"     | "今日建议"   | "明日建议"   |
| **展示的预测**    | T日预测        | T日预测      | T+1日预测    |
| **验证结果**      | ❌ 隐藏         | ❌ 隐藏       | ✅ 显示       |
| **价格数据**      | ❌ 隐藏（留白） | ✅ 显示实时价 | ✅ 显示收盘价 |
| **止损/支撑价**   | ✅ 显示         | ✅ 显示       | ✅ 显示       |
| **市场情绪(RSI)** | ❌ 隐藏         | ✅ 显示       | ✅ 显示       |

### 3.2 文案规范

#### 主标题
- S1/S2: `今日建议`
- S3: `明日建议`

#### 信号文案
| AI Signal | 显示文案 | 颜色             |
| --------- | -------- | ---------------- |
| Long      | 做多     | 🟢 绿色 (#10b981) |
| Side      | 观望     | 🟡 黄色 (#f59e0b) |
| Short     | 避险     | 🔴 红色 (#f43f5e) |

#### 验证结果（仅收市后显示）
| 状态      | 显示文案               |
| --------- | ---------------------- |
| Correct   | "今日验证: ✅ 预测准确" |
| Incorrect | "今日验证: ❌ 需要回顾" |
| Pending   | 不显示                 |

---

## 四、数据获取逻辑

### 4.1 后端 API（无需修改）

当前 API 返回:
- `prediction`: 最新一条 AI 预测
- `previousPrediction`: 上一条 AI 预测

### 4.2 前端显示逻辑

```
场景判断:
├─ 当前时间 >= 16:00 → 收市后 (S3)
│   ├─ 显示 prediction (明日预测)
│   └─ 显示 previousPrediction.validation_status (今日验证)
│
└─ 当前时间 < 16:00 → 开市前/交易中 (S1/S2)
    ├─ 显示 previousPrediction (今日预测，因为是昨天生成的)
    └─ 隐藏验证结果
```

#### 关键理解

- **收市后生成的预测**：`date = 今天`，`target_date = 明天`
- **用户第二天查看**：这条预测变成了"今日预测"

因此：
- S3 收市后：显示 `prediction`（刚生成，预测明天）
- S1/S2 开市前/中：显示 `previousPrediction`（昨天生成，预测今天）

---

## 五、价格数据显示规则

| 场景      | 显示内容            | 说明               |
| --------- | ------------------- | ------------------ |
| S1 开市前 | 不显示              | 极简处理，聚焦预测 |
| S2 交易中 | 当日实时价 + 涨跌幅 | 每10分钟更新       |
| S3 收市后 | 当日收盘价 + 涨跌幅 | 固定显示           |

---

## 六、历史回顾

- 用户可**上滑**查看历史预测卡片
- 每张历史卡片包含：日期、预测信号、验证结果、实盘变动
- 这替代了"昨日验证"在开市场景的功能，避免信息冗余

---

## 七、实现检查清单

- [ ] 前端增加时间场景判断函数
- [ ] 修改 StockDashboardCard 组件的显示逻辑
- [ ] 更新主标题文案动态切换
- [ ] 开市前隐藏价格区块
- [ ] 开市前/中隐藏验证结果
- [ ] 测试三个时间场景的显示效果

---

*文档作者: AI Agent (Antigravity)*  
*最后更新: 2025-12-26*
